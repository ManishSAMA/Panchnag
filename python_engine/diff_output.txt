diff --git a/static/app.css b/static/app.css
index 9b207c5..c9378a0 100644
--- a/static/app.css
+++ b/static/app.css
@@ -5,51 +5,65 @@
    ========================================================= */
 
 :root {
-  --red:         #8B1A1A;
-  --red-dark:    #6B0E0E;
-  --gold:        #F5D080;
-  --gold-light:  #FDF0C0;
-  --gold-dark:   #F0C040;
-  --olive:       #6B6B00;
-  --olive-dark:  #4A4A00;
-  --white:       #FFFFFF;
-  --text-dark:   #1A1A1A;
-  --text-muted:  #555555;
-  --text-red:    #8B1A1A;
-  --green-slot:  #2E7D32;
-  --red-slot:    #B71C1C;
-  --gray-slot:   #616161;
-  --border:      rgba(139, 26, 26, 0.18);
-
-  --navbar-h:    52px;
-  --max-w:       480px;
-  --radius:      10px;
-  --shadow:      0 2px 12px rgba(0,0,0,0.12);
+  --bg:           #f3ebe0;
+  --surface:      #ffffff;
+  --surface-soft: #fbf6f1;
+  --surface-muted:#f6ede5;
+  --brand:        #8c2a2a;
+  --brand-dark:   #5f1a1a;
+  --accent:       #d19c53;
+  --accent-soft:  #f7ead1;
+  --accent-strong:#b47a3b;
+  --white:        #ffffff;
+  --red:          #8c2a2a;
+  --green:        #5b6d38;
+  --green-slot:   #5d7332;
+  --red-slot:     #9a2b20;
+  --gray-slot:    #5b5850;
+  --text-red:     #8c2a2a;
+  --olive:        #5b6d38;
+  --olive-dark:   #3e4a24;
+  --text:         #2a2824;
+  --text-muted:   #6f6a61;
+  --text-soft:    #958b82;
+  --border:       rgba(88, 61, 31, 0.14);
+  --shadow:       0 18px 40px rgba(38, 33, 28, 0.08);
+  --shadow-soft:  0 10px 22px rgba(38, 33, 28, 0.06);
+  --navbar-h:     56px;
+  --max-w:        540px;
+  --radius:       20px;
 }
 
 /*  Reset & Base  */
 *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
 
 html, body {
-  height: 100%;
+  min-height: 100%;
   font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
   font-size: 15px;
-  background: var(--gold);
-  color: var(--text-dark);
+  background: radial-gradient(circle at top, rgba(244,228,210,0.98) 0%, rgba(243,235,224,1) 40%, rgba(245,238,232,1) 100%);
+  color: var(--text);
   overscroll-behavior: none;
 }
 
-body { display: flex; flex-direction: column; align-items: center; }
+body {
+  display: flex;
+  justify-content: center;
+  padding: 16px 0 24px;
+  min-height: 100vh;
+}
 
 #app {
   width: 100%;
   max-width: var(--max-w);
-  min-height: 100vh;
+  min-height: calc(100vh - 40px);
   display: flex;
   flex-direction: column;
-  background: var(--gold-light);
+  background: var(--surface);
   position: relative;
-  box-shadow: 0 0 40px rgba(0,0,0,0.2);
+  border-radius: 28px 28px 0 0;
+  box-shadow: var(--shadow);
+  overflow: hidden;
 }
 
 /*  Pages  */
@@ -61,51 +75,51 @@ body { display: flex; flex-direction: column; align-items: center; }
   position: sticky;
   top: 0;
   z-index: 100;
-  background: var(--red);
-  color: var(--white);
+  background: linear-gradient(135deg, rgba(140,42,42,0.92), rgba(114,39,39,0.95));
+  color: var(--surface);
   height: var(--navbar-h);
   display: flex;
   align-items: center;
-  padding: 0 12px;
+  padding: 0 14px;
   gap: 8px;
-  box-shadow: 0 2px 8px rgba(0,0,0,0.3);
+  box-shadow: 0 18px 30px rgba(0,0,0,0.12);
   flex-shrink: 0;
 }
 
 .navbar-btn {
-  background: none;
+  background: rgba(255,255,255,0.08);
   border: none;
-  color: var(--white);
+  color: var(--surface);
   font-size: 20px;
   cursor: pointer;
-  padding: 8px;
+  padding: 10px;
   border-radius: 50%;
   line-height: 1;
   display: flex;
   align-items: center;
   justify-content: center;
-  min-width: 40px;
-  min-height: 40px;
-  transition: background 0.15s;
+  min-width: 44px;
+  min-height: 44px;
+  transition: background 0.2s ease, transform 0.2s ease;
 }
-.navbar-btn:hover { background: rgba(255,255,255,0.15); }
+.navbar-btn:hover { background: rgba(255,255,255,0.18); transform: translateY(-1px); }
 
 .navbar-title {
   flex: 1;
   text-align: center;
   font-size: 17px;
   font-weight: 700;
-  letter-spacing: 0.3px;
+  letter-spacing: 0.2px;
 }
 
-.navbar-right { display: flex; gap: 0; }
+.navbar-right { display: flex; gap: 4px; }
 
 /*  Hamburger Drawer  */
 .drawer-overlay {
   display: none;
   position: fixed;
   inset: 0;
-  background: rgba(0,0,0,0.5);
+  background: rgba(0,0,0,0.35);
   z-index: 200;
 }
 .drawer-overlay.open { display: block; }
@@ -113,20 +127,21 @@ body { display: flex; flex-direction: column; align-items: center; }
 .drawer {
   position: fixed;
   top: 0; left: 0; bottom: 0;
-  width: 280px;
-  background: var(--white);
+  width: 260px;
+  background: var(--surface);
   z-index: 201;
   transform: translateX(-100%);
   transition: transform 0.25s ease;
   display: flex;
   flex-direction: column;
+  border-right: 1px solid rgba(0,0,0,0.06);
 }
 .drawer.open { transform: translateX(0); }
 
 .drawer-header {
-  background: var(--red);
-  color: var(--white);
-  padding: 20px 16px 16px;
+  background: linear-gradient(135deg, var(--brand), var(--brand-dark));
+  color: var(--surface);
+  padding: 22px 18px 18px;
   font-size: 18px;
   font-weight: 700;
 }
@@ -134,112 +149,170 @@ body { display: flex; flex-direction: column; align-items: center; }
   display: block;
   font-size: 12px;
   font-weight: 400;
-  opacity: 0.8;
-  margin-top: 2px;
+  opacity: 0.85;
+  margin-top: 4px;
 }
 
-.drawer-nav { flex: 1; overflow-y: auto; padding: 8px 0; }
+.drawer-nav { flex: 1; overflow-y: auto; padding: 10px 0; }
 
 .drawer-link {
   display: flex;
   align-items: center;
   gap: 14px;
-  padding: 14px 20px;
-  color: var(--text-dark);
+  padding: 14px 18px;
+  color: var(--text);
   text-decoration: none;
   font-size: 15px;
   border-bottom: 1px solid rgba(0,0,0,0.06);
   cursor: pointer;
   background: none;
-  border-left: none;
-  border-right: none;
-  border-top: none;
   width: 100%;
-  text-align: left;
+  transition: background 0.2s ease;
 }
-.drawer-link:hover { background: var(--gold-light); }
-.drawer-link .icon { font-size: 20px; width: 26px; text-align: center; }
+.drawer-link:hover { background: var(--surface-soft); }
+.drawer-link .icon { font-size: 20px; width: 28px; text-align: center; }
 
-/*  Date Banner (olive)  */
+/*  Date Banner  */
 .date-banner {
-  background: var(--olive);
-  color: var(--white);
-  padding: 12px 16px;
+  background: linear-gradient(135deg, var(--olive), #7b8b44);
+  color: var(--surface);
+  padding: 16px 18px;
   display: flex;
-  gap: 14px;
+  gap: 16px;
   align-items: center;
   flex-shrink: 0;
 }
 
-.date-banner-moon { font-size: 32px; line-height: 1; }
+.date-banner-moon { font-size: 36px; line-height: 1; }
 
 .date-banner-info { flex: 1; min-width: 0; }
 
 .date-banner-gregorian {
-  font-size: 22px;
-  font-weight: 700;
-  line-height: 1.1;
+  font-size: 20px;
+  font-weight: 800;
+  line-height: 1.15;
   white-space: nowrap;
   overflow: hidden;
   text-overflow: ellipsis;
 }
 
 .date-banner-details {
-  font-size: 11px;
-  opacity: 0.85;
-  margin-top: 3px;
-  line-height: 1.4;
+  font-size: 12px;
+  opacity: 0.95;
+  margin-top: 5px;
+  line-height: 1.5;
 }
 
 /*  Card Grid  */
 .card-grid {
   display: grid;
-  grid-template-columns: 1fr 1fr;
-  gap: 12px;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  gap: 14px;
   padding: 16px;
 }
 
 .card {
-  background: var(--gold);
-  border: 1.5px solid var(--gold-dark);
-  border-radius: var(--radius);
-  padding: 16px 12px;
+  background: var(--surface);
+  border: 1px solid rgba(140, 90, 42, 0.12);
+  border-radius: 18px;
+  padding: 18px 14px;
   cursor: pointer;
   text-align: center;
-  transition: transform 0.1s, box-shadow 0.1s;
+  transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
   text-decoration: none;
   display: flex;
   flex-direction: column;
   align-items: center;
-  gap: 6px;
-  box-shadow: var(--shadow);
+  gap: 10px;
+  box-shadow: var(--shadow-soft);
+}
+.card:hover { transform: translateY(-2px); border-color: rgba(140,90,42,0.2); }
+.card:active { transform: scale(0.98); }
+
+.card-icon {
+  font-size: 30px;
+  line-height: 1;
+  padding: 14px;
+  border-radius: 16px;
+  background: var(--accent-soft);
+}
+.card-title {
+  font-size: 14px;
+  font-weight: 700;
+  color: var(--brand);
+}
+.card-desc {
+  font-size: 12px;
+  color: var(--text-muted);
+  line-height: 1.45;
 }
-.card:active { transform: scale(0.97); }
 
-.card-icon { font-size: 28px; line-height: 1; }
-.card-title { font-size: 13px; font-weight: 700; color: var(--text-red); }
-.card-desc { font-size: 11px; color: var(--text-muted); line-height: 1.3; }
+.home-daily-panel {
+  background: var(--surface);
+  border: 1px solid rgba(140, 90, 42, 0.12);
+  border-left: 4px solid var(--accent);
+  border-radius: 20px;
+  padding: 18px 18px;
+  cursor: pointer;
+  transition: transform 0.18s ease, box-shadow 0.18s ease;
+  box-shadow: var(--shadow-soft);
+}
+.home-daily-panel:hover { transform: translateY(-1px); }
+.home-daily-label {
+  font-size: 12px;
+  font-weight: 700;
+  letter-spacing: 0.5px;
+  color: var(--brand-dark);
+  text-transform: uppercase;
+}
+.home-daily-tithi {
+  margin-top: 10px;
+  font-size: 26px;
+  font-weight: 800;
+  color: var(--text);
+}
+.home-daily-meta {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 10px;
+  margin-top: 12px;
+  color: var(--text-muted);
+  font-size: 13px;
+}
+.home-daily-empty {
+  padding: 24px 18px;
+  background: var(--surface);
+  border: 1px solid rgba(140,90,42,0.12);
+  border-radius: 20px;
+  color: var(--text-muted);
+  text-align: center;
+  box-shadow: var(--shadow-soft);
+}
 
 /*  Section / Page content  */
-.page-content { flex: 1; overflow-y: auto; }
+.page-content {
+  flex: 1;
+  overflow-y: auto;
+  padding-bottom: 20px;
+}
 
 /*  Calendar Page  */
 .cal-month-banner {
-  background: var(--olive);
-  color: var(--white);
-  padding: 10px 16px;
+  background: linear-gradient(135deg, #5a7139, #7e9561);
+  color: var(--surface);
+  padding: 14px 18px;
   text-align: center;
   flex-shrink: 0;
 }
-.cal-month-banner h2 { font-size: 18px; font-weight: 700; }
-.cal-month-banner small { font-size: 11px; opacity: 0.85; }
+.cal-month-banner h2 { font-size: 20px; font-weight: 700; }
+.cal-month-banner small { font-size: 12px; opacity: 0.92; margin-top: 4px; display: inline-block; }
 
 .cal-toolbar {
   display: flex;
-  gap: 8px;
-  padding: 10px 12px;
+  gap: 10px;
+  padding: 12px 14px;
   overflow-x: auto;
-  background: var(--white);
+  background: var(--surface);
   border-bottom: 1px solid var(--border);
   flex-shrink: 0;
   scrollbar-width: none;
@@ -247,57 +320,59 @@ body { display: flex; flex-direction: column; align-items: center; }
 .cal-toolbar::-webkit-scrollbar { display: none; }
 
 .toolbar-btn {
-  background: var(--gold);
-  border: 1.5px solid var(--gold-dark);
-  border-radius: 20px;
-  padding: 6px 14px;
-  font-size: 12px;
-  font-weight: 600;
-  color: var(--text-red);
+  background: var(--surface-soft);
+  border: 1px solid rgba(140,90,42,0.16);
+  border-radius: 999px;
+  padding: 8px 16px;
+  font-size: 13px;
+  font-weight: 700;
+  color: var(--text);
   cursor: pointer;
   white-space: nowrap;
   flex-shrink: 0;
+  transition: background 0.2s ease, color 0.2s ease;
 }
 .toolbar-btn.active {
   background: var(--olive);
-  color: var(--white);
-  border-color: var(--olive);
+  color: var(--surface);
+  border-color: rgba(91,109,56,0.3);
 }
 
 .cal-nav-row {
   display: flex;
   align-items: center;
   justify-content: space-between;
-  padding: 10px 16px;
-  background: var(--gold-light);
+  padding: 12px 16px;
+  background: var(--surface-soft);
   border-bottom: 1px solid var(--border);
   flex-shrink: 0;
 }
 
-.cal-nav-row h3 { font-size: 16px; font-weight: 700; color: var(--text-red); }
+.cal-nav-row h3 { font-size: 16px; font-weight: 700; color: var(--brand-dark); }
 
 .nav-arrow {
-  background: none;
-  border: none;
+  background: var(--surface);
+  border: 1px solid rgba(140,90,42,0.15);
   font-size: 20px;
-  color: var(--red);
+  color: var(--brand-dark);
   cursor: pointer;
-  padding: 6px 10px;
-  border-radius: 6px;
+  padding: 8px 12px;
+  border-radius: 12px;
+  transition: background 0.2s ease;
 }
-.nav-arrow:hover { background: var(--gold); }
+.nav-arrow:hover { background: var(--accent-soft); }
 
 .cal-weekdays {
   display: grid;
   grid-template-columns: repeat(7, 1fr);
-  background: var(--red);
-  color: var(--white);
+  background: rgba(91, 109, 56, 0.94);
+  color: var(--surface);
   text-align: center;
   font-size: 11px;
-  font-weight: 600;
+  font-weight: 700;
   flex-shrink: 0;
 }
-.cal-weekday { padding: 6px 2px; }
+.cal-weekday { padding: 8px 4px; }
 
 .cal-grid {
   display: grid;
@@ -308,19 +383,19 @@ body { display: flex; flex-direction: column; align-items: center; }
 }
 
 .cal-cell {
-  background: var(--gold-light);
-  padding: 4px 3px;
-  min-height: 72px;
+  background: var(--surface);
+  padding: 8px 8px;
+  min-height: 78px;
   cursor: pointer;
   display: flex;
   flex-direction: column;
-  transition: background 0.1s;
+  transition: background 0.2s ease, transform 0.2s ease;
 }
-.cal-cell:hover { background: var(--gold); }
-.cal-cell.empty { background: rgba(255,255,255,0.4); cursor: default; pointer-events: none; }
+.cal-cell:hover { background: var(--surface-soft); transform: translateY(-1px); }
+.cal-cell.empty { background: rgba(255,255,255,0.64); cursor: default; pointer-events: none; }
 .cal-cell.today {
-  background: #d4e6a0;
-  outline: 2px solid var(--olive);
+  background: #f4f5d5;
+  outline: 2px solid rgba(91,109,56,0.8);
   outline-offset: -2px;
 }
 
@@ -333,11 +408,42 @@ body { display: flex; flex-direction: column; align-items: center; }
 .cal-date {
   font-size: 14px;
   font-weight: 700;
-  color: var(--text-dark);
-  line-height: 1;
+  color: var(--text);
+  line-height: 1.1;
 }
 .cal-cell.today .cal-date { color: var(--olive-dark); }
 
+.cal-tithi {
+  font-size: 9px;
+  color: var(--text-muted);
+  font-weight: 600;
+  margin-top: 4px;
+}
+
+.cal-cell-bottom {
+  display: flex;
+  justify-content: space-between;
+  margin-top: auto;
+}
+
+.cal-time {
+  font-size: 8px;
+  color: var(--text-soft);
+  line-height: 1.3;
+}
+
+.cal-nakshatra {
+  font-size: 9px;
+  color: var(--brand-dark);
+  font-weight: 600;
+  margin-top: 4px;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+.cal-loading { text-align: center; padding: 40px; color: var(--text-muted); }
+
 .cal-tithi { font-size: 9px; color: var(--text-muted); font-weight: 600; }
 
 .cal-cell-bottom {
@@ -368,71 +474,71 @@ body { display: flex; flex-direction: column; align-items: center; }
 .time-toggle {
   display: flex;
   gap: 0;
-  padding: 10px 16px;
-  background: var(--gold-light);
+  padding: 12px 16px;
+  background: var(--surface-soft);
   border-bottom: 1px solid var(--border);
   flex-shrink: 0;
 }
 
 .time-toggle-btn {
-  background: var(--gold);
-  border: 1.5px solid var(--gold-dark);
-  padding: 7px 18px;
+  background: var(--surface);
+  border: 1px solid rgba(140,90,42,0.16);
+  padding: 8px 18px;
   font-size: 13px;
-  font-weight: 600;
+  font-weight: 700;
   cursor: pointer;
-  color: var(--text-muted);
-  transition: background 0.15s;
+  color: var(--text);
+  transition: background 0.2s ease, color 0.2s ease;
 }
-.time-toggle-btn:first-child { border-radius: 6px 0 0 6px; }
-.time-toggle-btn:last-child  { border-radius: 0 6px 6px 0; border-left: none; }
+.time-toggle-btn:first-child { border-radius: 14px 0 0 14px; }
+.time-toggle-btn:last-child { border-radius: 0 14px 14px 0; border-left: none; }
 .time-toggle-btn.active {
-  background: var(--red);
-  color: var(--white);
-  border-color: var(--red);
+  background: var(--brand);
+  color: var(--surface);
+  border-color: var(--brand);
 }
 
 .panchang-card {
-  background: var(--gold);
+  background: var(--surface);
   border-radius: var(--radius);
-  margin: 14px;
+  margin: 16px;
   overflow: hidden;
-  box-shadow: var(--shadow);
-  border: 1.5px solid var(--gold-dark);
+  box-shadow: var(--shadow-soft);
+  border: 1px solid rgba(140,90,42,0.12);
 }
 
 .panchang-sun-moon {
   display: grid;
   grid-template-columns: 1fr 1fr;
-  border-bottom: 1px solid var(--gold-dark);
+  border-bottom: 1px solid rgba(140,90,42,0.12);
 }
 
 .sun-moon-cell {
-  padding: 12px 14px;
+  padding: 16px 18px;
   text-align: center;
 }
-.sun-moon-cell:first-child { border-right: 1px solid var(--gold-dark); }
+.sun-moon-cell:first-child { border-right: 1px solid rgba(140,90,42,0.12); }
 
 .sun-moon-label {
   font-size: 11px;
-  color: var(--text-red);
-  font-weight: 700;
+  color: var(--brand);
+  font-weight: 800;
   text-transform: uppercase;
   letter-spacing: 0.5px;
 }
 
 .sun-moon-time {
   font-size: 18px;
-  font-weight: 700;
-  color: var(--text-dark);
-  margin-top: 2px;
+  font-weight: 800;
+  color: var(--text);
+  margin-top: 4px;
 }
 
 .panchang-row {
   display: flex;
   align-items: flex-start;
-  border-bottom: 1px solid rgba(176, 140, 60, 0.3);
-  padding: 10px 14px;
+  border-bottom: 1px solid rgba(140, 90, 42, 0.08);
+  padding: 14px 18px;
   gap: 10px;
 }
 .panchang-row:last-child { border-bottom: none; }
@@ -440,17 +546,17 @@ body { display: flex; flex-direction: column; align-items: center; }
 .panchang-label {
   font-size: 12px;
   font-weight: 700;
-  color: var(--text-red);
-  min-width: 90px;
+  color: var(--brand);
+  min-width: 100px;
   flex-shrink: 0;
-  padding-top: 1px;
+  padding-top: 2px;
 }
 
 .panchang-value {
   font-size: 14px;
-  color: var(--text-dark);
+  color: var(--text);
   flex: 1;
-  line-height: 1.5;
+  line-height: 1.6;
 }
 
 .panchang-endtime {
@@ -459,18 +565,41 @@ body { display: flex; flex-direction: column; align-items: center; }
 }
 
 /*  Rahu Kaal Card  */
-.rahu-kaal-card  { border-left: 4px solid #c0392b; }
-.rahu-kaal-title { font-weight: bold; color: #c0392b; font-size: 1.05em; padding: 6px 0 4px; }
-.rahu-kaal-note  { font-size: 11px; color: var(--text-muted); padding: 4px 0 6px; }
-.rahu-badge      { font-size: 11px; border-radius: 4px; padding: 1px 6px; margin-left: 6px; }
-.rahu-badge--active   { background: #fdecea; color: #c0392b; }
-.rahu-badge--inactive { background: #eafaf1; color: #27ae60; }
+.rahu-kaal-card {
+  border-left: 4px solid var(--red);
+  background: rgba(248, 237, 201, 0.95);
+}
+.rahu-kaal-title {
+  font-weight: 800;
+  color: var(--red);
+  font-size: 1.05em;
+  padding: 8px 0 4px;
+}
+.rahu-kaal-note {
+  font-size: 12px;
+  color: var(--text-muted);
+  padding: 4px 0 6px;
+}
+.rahu-badge {
+  font-size: 11px;
+  border-radius: 6px;
+  padding: 2px 8px;
+  margin-left: 6px;
+}
+.rahu-badge--active {
+  background: rgba(255, 230, 230, 0.9);
+  color: var(--red);
+}
+.rahu-badge--inactive {
+  background: rgba(217, 238, 229, 0.9);
+  color: var(--green);
+}
 
 /*  Choghadiya Page  */
 .chog-banner {
-  background: var(--gold);
-  border-bottom: 2px solid var(--gold-dark);
-  padding: 12px 14px;
+  background: var(--surface);
+  border-bottom: 1px solid rgba(140,90,42,0.16);
+  padding: 14px;
   display: flex;
   gap: 12px;
   align-items: stretch;
@@ -478,184 +607,218 @@ body { display: flex; flex-direction: column; align-items: center; }
 }
 
 .chog-banner-left { flex: 1; }
-.chog-banner-time { font-size: 13px; font-weight: 600; color: var(--text-muted); }
-.chog-banner-countdown {
-  font-size: 28px;
+.chog-banner-time {
+  font-size: 13px;
   font-weight: 700;
-  color: var(--text-dark);
+  color: var(--text-muted);
+}
+.chog-banner-countdown {
+  font-size: 30px;
+  font-weight: 800;
+  color: var(--text);
   font-variant-numeric: tabular-nums;
-  letter-spacing: 1px;
+  letter-spacing: 0;
+}
+.chog-banner-date {
+  font-size: 12px;
+  color: var(--text-muted);
+  margin-top: 4px;
 }
-.chog-banner-date { font-size: 11px; color: var(--text-muted); margin-top: 2px; }
 
 .chog-banner-right {
   background: var(--green-slot);
   color: var(--white);
-  border-radius: 8px;
-  padding: 10px 12px;
+  border-radius: 16px;
+  padding: 14px 12px;
   text-align: center;
-  min-width: 110px;
+  min-width: 120px;
   display: flex;
   flex-direction: column;
   justify-content: center;
   gap: 4px;
 }
-.chog-banner-name { font-size: 15px; font-weight: 700; }
-.chog-banner-quality { font-size: 11px; opacity: 0.9; }
+.chog-banner-name {
+  font-size: 16px;
+  font-weight: 800;
+}
+.chog-banner-quality {
+  font-size: 12px;
+  opacity: 0.9;
+}
 
-.chog-time-toggle {
+.chog-time-toggle,
+.chog-date-nav,
+.weekday-tabs {
   display: flex;
   gap: 0;
-  padding: 8px 14px;
-  background: var(--gold-light);
-  border-bottom: 1px solid var(--border);
+  padding: 10px 14px;
+  background: var(--surface-soft);
+  border-bottom: 1px solid rgba(140,90,42,0.12);
   flex-shrink: 0;
 }
 
 .chog-date-nav {
-  display: flex;
-  align-items: center;
   justify-content: space-between;
-  padding: 8px 12px;
-  background: var(--gold-light);
-  border-bottom: 1px solid var(--border);
-  flex-shrink: 0;
 }
 
-.chog-date-label { font-size: 14px; font-weight: 700; color: var(--text-dark); }
+.chog-date-label {
+  font-size: 14px;
+  font-weight: 700;
+  color: var(--text);
+}
 
 .weekday-tabs {
-  display: flex;
-  gap: 4px;
-  padding: 6px 12px 8px;
-  background: var(--gold-light);
-  border-bottom: 1px solid var(--border);
+  align-items: center;
   overflow-x: auto;
-  flex-shrink: 0;
   scrollbar-width: none;
 }
 .weekday-tabs::-webkit-scrollbar { display: none; }
 
 .weekday-tab {
-  background: var(--gold);
-  border: 1.5px solid var(--gold-dark);
-  border-radius: 20px;
-  padding: 4px 10px;
+  background: var(--surface);
+  border: 1px solid rgba(140,90,42,0.16);
+  border-radius: 18px;
+  padding: 6px 12px;
   font-size: 12px;
-  font-weight: 600;
+  font-weight: 700;
   color: var(--text-muted);
   cursor: pointer;
   white-space: nowrap;
   flex-shrink: 0;
 }
 .weekday-tab.active {
-  background: var(--olive);
-  color: var(--white);
-  border-color: var(--olive);
+  background: var(--brand);
+  color: var(--surface);
+  border-color: var(--brand);
 }
 
 .chog-section-header {
   display: flex;
   justify-content: space-between;
   align-items: center;
-  background: var(--gold);
-  padding: 8px 14px;
-  border-top: 1px solid var(--gold-dark);
-  border-bottom: 1px solid var(--gold-dark);
+  background: rgba(255,249,232,0.95);
+  padding: 10px 14px;
+  border-top: 1px solid rgba(140,90,42,0.14);
+  border-bottom: 1px solid rgba(140,90,42,0.14);
   font-size: 13px;
   font-weight: 700;
-  color: var(--text-dark);
+  color: var(--text);
   flex-shrink: 0;
 }
-.chog-section-header span { color: var(--text-muted); font-weight: 400; font-size: 12px; }
+.chog-section-header span {
+  color: var(--text-muted);
+  font-weight: 400;
+  font-size: 12px;
+}
 
 .chog-slot {
   display: flex;
-  border-bottom: 1px solid rgba(0,0,0,0.06);
+  border-bottom: 1px solid rgba(140,90,42,0.08);
 }
 
 .chog-slot-name {
   width: 42%;
-  padding: 11px 10px;
+  padding: 12px 12px;
   display: flex;
   align-items: center;
   justify-content: space-between;
   color: var(--white);
   font-size: 13px;
-  font-weight: 700;
-  gap: 4px;
+  font-weight: 800;
+  gap: 6px;
   flex-shrink: 0;
 }
-.chog-slot-name-text { flex: 1; line-height: 1.3; }
-.chog-slot-name-text small { display: block; font-size: 10px; font-weight: 400; opacity: 0.85; }
+.chog-slot-name-text {
+  flex: 1;
+  line-height: 1.25;
+}
+.chog-slot-name-text small {
+  display: block;
+  font-size: 10px;
+  font-weight: 400;
+  opacity: 0.85;
+}
 
-.chog-slot.auspicious   .chog-slot-name { background: var(--green-slot); }
+.chog-slot.auspicious .chog-slot-name { background: var(--green-slot); }
 .chog-slot.inauspicious .chog-slot-name { background: var(--red-slot); }
-.chog-slot.neutral      .chog-slot-name { background: var(--gray-slot); }
+.chog-slot.neutral .chog-slot-name { background: var(--gray-slot); }
 
 .chog-slot-time {
   flex: 1;
-  padding: 11px 10px;
+  padding: 12px 14px;
   display: flex;
   align-items: center;
   justify-content: space-between;
-  background: var(--gold-light);
-  font-size: 12px;
-  color: var(--text-dark);
+  background: rgba(255,249,232,0.9);
+  font-size: 13px;
+  color: var(--text);
 }
 
 .chog-slot.current .chog-slot-time {
-  background: var(--gold);
-  font-weight: 700;
+  background: rgba(255,238,175,0.95);
+  color: var(--brand);
+  font-weight: 800;
 }
 
-.chog-slot-icon { font-size: 16px; }
+.chog-slot-icon {
+  font-size: 16px;
+}
 
 /*  Location Page  */
 .location-form {
-  padding: 16px;
+  padding: 18px;
   display: flex;
   flex-direction: column;
-  gap: 12px;
+  gap: 14px;
 }
 
 .search-box {
   position: relative;
 }
 
-.search-input {
+.search-input,
+.form-input,
+.form-select {
   width: 100%;
-  border: 2px solid var(--gold-dark);
-  border-radius: 8px;
-  padding: 11px 14px;
+  border: 1.5px solid rgba(140,90,42,0.16);
+  border-radius: 14px;
+  padding: 12px 14px;
   font-size: 15px;
-  background: var(--white);
-  color: var(--text-dark);
+  background: #fffdf4;
+  color: var(--text);
   outline: none;
 }
-.search-input:focus { border-color: var(--red); }
+.search-input:focus,
+.form-input:focus,
+.form-select:focus {
+  border-color: var(--brand);
+  box-shadow: 0 0 0 4px rgba(205, 140, 48, 0.12);
+}
 
 .search-results {
   position: absolute;
   top: 100%;
-  left: 0; right: 0;
-  background: var(--white);
-  border: 1.5px solid var(--gold-dark);
+  left: 0;
+  right: 0;
+  background: var(--surface);
+  border: 1px solid rgba(140,90,42,0.16);
   border-top: none;
-  border-radius: 0 0 8px 8px;
+  border-radius: 0 0 12px 12px;
   z-index: 50;
-  max-height: 200px;
+  max-height: 220px;
   overflow-y: auto;
-  box-shadow: var(--shadow);
+  box-shadow: var(--shadow-soft);
 }
 
 .search-result-item {
-  padding: 11px 14px;
+  padding: 12px 14px;
   cursor: pointer;
   border-bottom: 1px solid rgba(0,0,0,0.06);
   font-size: 13px;
 }
-.search-result-item:hover { background: var(--gold-light); }
+.search-result-item:hover {
+  background: rgba(248,237,201,0.9);
+}
 
 .form-divider {
   text-align: center;
@@ -663,134 +826,158 @@ body { display: flex; flex-direction: column; align-items: center; }
   font-size: 12px;
   position: relative;
 }
-.form-divider::before, .form-divider::after {
+.form-divider::before,
+.form-divider::after {
   content: '';
   position: absolute;
   top: 50%;
   width: 40%;
   height: 1px;
-  background: var(--border);
+  background: rgba(140,90,42,0.16);
 }
 .form-divider::before { left: 0; }
 .form-divider::after { right: 0; }
 
-.form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
+.form-row {
+  display: grid;
+  grid-template-columns: 1fr 1fr;
+  gap: 12px;
+}
 
-.form-group { display: flex; flex-direction: column; gap: 4px; }
+.form-group {
+  display: flex;
+  flex-direction: column;
+  gap: 6px;
+}
 
 .form-label {
   font-size: 12px;
   font-weight: 700;
-  color: var(--text-red);
-}
-
-.form-input {
-  border: 2px solid var(--gold-dark);
-  border-radius: 8px;
-  padding: 9px 12px;
-  font-size: 14px;
-  background: var(--white);
-  color: var(--text-dark);
-  outline: none;
-  width: 100%;
+  color: var(--brand);
 }
-.form-input:focus { border-color: var(--red); }
 
 .btn-primary {
-  background: var(--red);
-  color: var(--white);
+  background: var(--brand);
+  color: var(--surface);
   border: none;
-  border-radius: 8px;
-  padding: 13px;
+  border-radius: 14px;
+  padding: 14px;
   font-size: 15px;
   font-weight: 700;
   cursor: pointer;
-  transition: background 0.15s;
+  transition: background 0.15s ease;
+}
+.btn-primary:hover {
+  background: #9f5c03;
 }
-.btn-primary:hover { background: var(--red-dark); }
 
 .location-current {
-  background: var(--gold);
-  border: 1.5px solid var(--gold-dark);
+  background: rgba(255,249,232,0.95);
+  border: 1px solid rgba(140,90,42,0.16);
   border-radius: var(--radius);
-  padding: 14px;
+  padding: 16px;
   font-size: 13px;
   color: var(--text-muted);
 }
-.location-current strong { color: var(--text-dark); display: block; font-size: 15px; margin-bottom: 2px; }
+.location-current strong {
+  color: var(--text);
+  display: block;
+  font-size: 15px;
+  margin-bottom: 2px;
+}
 
 /*  Settings Page  */
 .settings-section {
-  padding: 14px 16px 0;
+  padding: 18px 16px 0;
 }
 .settings-section-title {
   font-size: 12px;
   font-weight: 700;
-  color: var(--text-red);
+  color: var(--brand);
   text-transform: uppercase;
   letter-spacing: 0.8px;
-  margin-bottom: 8px;
+  margin-bottom: 10px;
 }
 
 .settings-card {
-  background: var(--gold);
-  border: 1.5px solid var(--gold-dark);
+  background: rgba(255,249,232,0.95);
+  border: 1px solid rgba(140,90,42,0.16);
   border-radius: var(--radius);
-  padding: 14px;
-  margin-bottom: 14px;
+  padding: 16px;
+  margin-bottom: 16px;
   display: flex;
   flex-direction: column;
-  gap: 12px;
-  box-shadow: var(--shadow);
+  gap: 14px;
+  box-shadow: var(--shadow-soft);
 }
 
 .settings-row {
   display: flex;
   align-items: center;
   justify-content: space-between;
-  gap: 10px;
+  gap: 12px;
 }
 
-.settings-label { font-size: 14px; font-weight: 600; color: var(--text-dark); }
+.settings-label {
+  font-size: 14px;
+  font-weight: 700;
+  color: var(--text);
+}
 
-.toggle-group { display: flex; gap: 0; }
+.toggle-group {
+  display: flex;
+  gap: 0;
+}
 
 .toggle-btn {
-  background: var(--gold-light);
-  border: 1.5px solid var(--gold-dark);
-  padding: 6px 14px;
+  background: var(--surface-soft);
+  border: 1px solid rgba(140,90,42,0.16);
+  padding: 8px 16px;
   font-size: 13px;
-  font-weight: 600;
+  font-weight: 700;
   cursor: pointer;
   color: var(--text-muted);
 }
-.toggle-btn:first-child { border-radius: 6px 0 0 6px; }
-.toggle-btn:last-child  { border-radius: 0 6px 6px 0; border-left: none; }
-.toggle-btn.active { background: var(--red); color: var(--white); border-color: var(--red); }
+.toggle-btn:first-child {
+  border-radius: 12px 0 0 12px;
+}
+.toggle-btn:last-child {
+  border-radius: 0 12px 12px 0;
+  border-left: none;
+}
+.toggle-btn.active {
+  background: var(--brand);
+  color: var(--surface);
+  border-color: var(--brand);
+}
 
 .form-select {
-  border: 2px solid var(--gold-dark);
-  border-radius: 8px;
-  padding: 7px 12px;
+  border: 1.5px solid rgba(140,90,42,0.16);
+  border-radius: 14px;
+  padding: 11px 14px;
   font-size: 14px;
-  background: var(--white);
-  color: var(--text-dark);
+  background: #fffdf4;
+  color: var(--text);
   outline: none;
   cursor: pointer;
 }
-.form-select:focus { border-color: var(--red); }
 
 .export-result {
-  background: var(--white);
-  border: 1px solid var(--gold-dark);
-  border-radius: 8px;
-  padding: 10px;
+  background: var(--surface);
+  border: 1px solid rgba(140,90,42,0.16);
+  border-radius: 12px;
+  padding: 12px 14px;
   font-size: 13px;
-  color: var(--text-dark);
+  color: var(--text);
   display: none;
 }
-.export-result.visible { display: block; }
-.export-result a { color: var(--red); font-weight: 600; }
+.export-result.visible {
+  display: block;
+}
+.export-result a {
+  color: var(--brand);
+  font-weight: 700;
+}
 
 /*  Loading / Error states  */
 .loading-spinner {
@@ -809,11 +996,3 @@ body { display: flex; flex-direction: column; align-items: center; }
   font-size: 13px;
   color: #8B0000;
 }
-
-/*  Scrollbar  */
-::-webkit-scrollbar { width: 4px; }
-::-webkit-scrollbar-thumb { background: var(--gold-dark); border-radius: 2px; }
-
-/*  Utilities  */
-.hidden { display: none !important; }
-.text-center { text-align: center; }
